[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader --no-scripts --no-plugins",
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = ["npm run build"]
