[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/mob-api/Dockerfile"
buildContext = "backend/mob-api"

[deploy]
startCommand = "php artisan migrate --force; php artisan serve --host=0.0.0.0 --port=$PORT"
