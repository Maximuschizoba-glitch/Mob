[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/mob-api/Dockerfile"
buildContext = "backend/mob-api"

[deploy]
startCommand = "php artisan package:discover --ansi 2>&1; echo 'DISCOVER_DONE'; php artisan serve --host=0.0.0.0 --port=$PORT"
